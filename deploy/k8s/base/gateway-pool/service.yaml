apiVersion: v1
kind: Service
metadata:
  name: gateway-pool
  labels:
    app.kubernetes.io/name: gateway-pool
    app.kubernetes.io/part-of: nexu
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  ports:
    - port: 18789
      targetPort: 18789
      protocol: TCP
      name: gateway
  selector:
    app.kubernetes.io/name: gateway-pool
