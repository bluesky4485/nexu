apiVersion: v1
kind: Service
metadata:
  name: nexu-api
  labels:
    app.kubernetes.io/name: nexu-api
    app.kubernetes.io/part-of: nexu
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: nexu-api
